using Microsoft.AspNetCore.Identity;

namespace Bakery.Models
{
    public class ApplicationUser : IdentityUser //custom class, part of Identity, gets user info
    {
        // holds blank user accounts
    }
}